---
  # Versions
  apollo_version: "2.0.4"

  # Paths
  apollo_home: "/mnt/gvl/apps/apollo"
  grails_home: "/mnt/gvl/.sdkman"
  nginx_config: "/etc/nginx/sites-enabled"
  tomcat_home: "/opt/tomcat8"
  tomcat_config: "{{ tomcat_home }}/conf"
  java_home: "/usr/lib/jvm/java-8-oracle"

  # Database info
  database_user: "database_user"
  database_prod: "apollo-production"
  database_pass: "postgres_password"
  database_user_chado: "database_user_chado"
  database_prod_chado: "apollo-production-chado"

  gvl_install_path: /opt/gvl
  apollo_port: "9650"
...
